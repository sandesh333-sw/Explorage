name: Explorage CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test --if-present
        env:
          CI: true

  build-and-push:
    needs: test
    runs-on: [self-hosted]          # build on Mac mini (ARM)
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        run: |
          docker build -t pulami/explorage:latest .
          docker tag pulami/explorage:latest pulami/explorage:${GITHUB_SHA}
          docker push pulami/explorage:latest
          docker push pulami/explorage:${GITHUB_SHA}

  deploy:
    needs: build-and-push
    runs-on: [self-hosted]  
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - name: Roll out new image on k3s
        run: |
          kubectl set image deployment/explorage-backend \
            backend=pulami/explorage:${GITHUB_SHA} \
            -n explorage

          kubectl rollout status deployment/explorage-backend -n explorage
          echo "✅ k3s rollout complete on Mac mini"
